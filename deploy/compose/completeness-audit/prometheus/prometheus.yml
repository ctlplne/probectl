# SPDX-License-Identifier: BUSL-1.1
global:
  scrape_interval: 30s
  evaluation_interval: 30s

# The audit writes two tenant-labeled samples over authenticated HTTPS remote
# write. It intentionally has no outbound scrape targets.
scrape_configs: []
