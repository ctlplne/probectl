# Flux source for the probectl chart (S35). Point url at the repo holding the chart.
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: probectl
  namespace: flux-system
spec:
  interval: 5m
  url: https://github.com/your-org/probectl.git
  ref:
    branch: main
