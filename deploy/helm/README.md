# deploy/helm/

Helm chart(s) for Kubernetes / OpenShift.

## Status (S0)

Placeholder. The chart — with **single-tenant** and **multi-tenant / provider**
reference values — is delivered as part of launch hardening in **S19**, and is
**HTTPS-by-default** (TLS-terminating ingress, HSTS, no plaintext API exposure;
CLAUDE.md §7 guardrail 12).
