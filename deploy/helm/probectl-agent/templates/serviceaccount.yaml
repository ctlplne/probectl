apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "probectl-agent.fullname" . }}
  labels:
    {{- include "probectl-agent.labels" . | nindent 4 }}
# The agent talks to the bus, never to the Kubernetes API.
automountServiceAccountToken: false
