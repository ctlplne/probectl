# SMALL reference values — lab / single-team / pilot.
#
#   helm install probectl deploy/helm/probectl -f deploy/helm/probectl/values-small.yaml \
#     --set ingress.host=probectl.example.com --set ingress.tlsSecretName=probectl-tls \
#     --set ingress.backendTLS.trustSecret=probectl-backend-ca \
#     --set ingress.backendTLS.serverName=probectl.example.com \
#     --set control.tls.existingSecret=probectl-tls \
#     --set-string image.digest='sha256:<release-digest>' \
#     --set secrets.envelopeKey=$(openssl rand -base64 32)
#
# One replica, modest resources. Same HTTPS-by-default + hardened pod posture as
# every profile — only the runtime sizing differs. No PDB/HPA (single replica).

replicaCount: 1

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: "1"
    memory: 512Mi

podDisruptionBudget:
  enabled: false

autoscaling:
  enabled: false
