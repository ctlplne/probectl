# deploy/terraform/

Reference Terraform modules for provisioning netctl infrastructure.

## Status (S0)

Placeholder. Reference modules are added alongside the production deployment
work in later phases. The directory exists now so the deploy layout matches
CLAUDE.md §5.
